# Cleany 발표자료 디자인 가이드

사용자가 좋은 PPT 사례로 제시한 세 참고 이미지와, 현재 발표자료의 반복적인 카드 구성을 개선하려는 요구를 바탕으로 작성했다.

이 문서는 **발표 슬라이드의 시각적 구성과 편집 기준**이다. Cleany 제품 UI의 디자인 시스템이나 제품·기술 사양을 정의하지 않는다. 참고 이미지에서 확인되는 특징과 이 문서가 제안하는 구현 수치를 구분한다.

## 1. 디자인 방향

**밝은 바탕, 선명한 파란색, 강한 제목, 충분한 여백을 공통 언어로 사용하고, 내용의 성격에 따라 사진·도식·차트·표의 구성을 바꾼다.**

핵심은 모든 정보를 같은 모양으로 포장하지 않는 것이다. 슬라이드마다 전달할 내용에 가장 잘 맞는 시각적 형식을 선택한다.

- 한 장에서 가장 먼저 보이는 요소를 하나 정한다. 핵심 문장, 사진, 수치, 차트, 도식 중 하나가 중심이 된다.
- 글자 크기와 여백으로 정보의 중요도를 구분한다. 모든 항목에 비슷한 크기의 박스를 배정하지 않는다.
- 동일한 색과 정렬 기준으로 통일감을 만들고, 화면 분할과 시각 자료의 비중으로 변화를 만든다.
- 카드는 독립된 항목을 묶을 이유가 있을 때 사용한다. 카드 사용 자체를 금지하지 않는다.
- 카드를 지운 자리에는 정보의 관계를 보여주는 구성을 만든다. 작은 글자만 넓게 흩어놓는 방식으로 끝내지 않는다.
- 내용이 많으면 핵심과 보조 설명을 정리하거나 장을 나눈다. 먼저 글자를 줄이지 않는다.

## 2. 참고 이미지에서 가져올 것

| 참고 | 관찰되는 특징 | Cleany에 적용할 원칙 |
|---|---|---|
| [참고 1 — 파란색 편집형 템플릿](docs/presentation-design/references/reference-01-blue-editorial.jpg) | 넓은 파란색 면, 비대칭 화면 분할, 큰 사진, 굵은 제목, 다양한 그래프와 순서도 | 표지·전환 장면의 과감한 색면, 사진 중심 구성, 슬라이드별 화면 변화 |
| [참고 2 — IR 데이터 구성](docs/presentation-design/references/reference-02-blue-ir-data.png) | 밝은 바탕, 정돈된 표와 차트, 선택적인 카드, 파랑·시안 그라데이션과 가장자리 장식 | 비교 데이터의 정렬, 강조 수치의 분리, 설명보다 결과를 크게 보여주는 구성 |
| [참고 3 — IR 레이아웃 구성](docs/presentation-design/references/reference-03-blue-ir-layouts.png) | 목차의 열린 열 배치, 큰 이미지와 본문 분할, 가로 타임라인, 인물·서비스에 맞춘 카드 | 정보 종류에 맞는 레이아웃 선택, 목록·흐름·독립 항목의 구분 |

세 이미지에는 카드도 많이 등장한다. 공통된 장점은 **카드·사진·표·도식의 역할이 구분되어 있다는 것**이다. 사용자가 지적한 문제는 카드 자체보다 ‘비슷한 박스 안에 글을 넣는 방식이 매 장 반복되는 것’으로 해석한다.

그대로 복제하지 않을 요소:

- 작은 썸네일에 보이는 극도로 작은 본문과 낮은 대비. 실제 발표 화면에서는 가독성을 별도로 검증한다.
- 참고 1의 영문 장식 서체를 한글 본문에 적용하는 것.
- 내용과 관계없는 건축 사진, 인물 실루엣, 지구본 등의 장식.
- 참고 자료의 예시 차트 수치와 기업 정보.
- 매 장 반복되는 그라데이션 덩어리, 물결 선, 그림자.

## 3. 시각적 기본값

다음 수치는 참고 이미지의 정확한 추출값이 아니라 **Cleany 발표자료 제작을 위한 권장 시작값**이다. 기존 자료의 일부 장만 수정할 때에는 주변 장의 브랜드 색과 제목 체계를 우선 유지한다.

### 색상

| 역할 | 권장값 | 사용 위치 |
|---|---|---|
| 기본 배경 | `#FFFFFF` | 일반 설명, 사진, 데이터 슬라이드 |
| 보조 배경 | `#F6F8FB` | 가벼운 영역 구분, 선택적 카드 |
| 제목·주요 본문 | `#102A43` | 제목, 항목명, 핵심 결론 |
| 보조 본문 | `#526579` | 설명, 캡션, 범례 |
| 기본 파란색 | `#238AF3` | 기존 Cleany 색상과 연결, 번호, 강조, 핵심 데이터 |
| 넓은 색면용 파란색 | `#176BEE` | 표지, 장 구분, 강조 영역 |
| 시안 | `#50CDF2` | 제한적인 그라데이션 끝색, 보조 강조 |
| 구분선·그리드 | `#D8E0E8` | 표의 행, 도식 연결, 얇은 구분선 |
| 어두운 배경 | `#0C2849` | 표지, 영상·시연 장면, 마무리 |

- 한 장에서는 기본 파란색 하나를 주로 사용한다. 파랑·남색·시안으로 모든 단어를 각각 칠하지 않는다.
- 일반 슬라이드는 중립색을 넓게 두고 핵심 요소만 강조한다. 전환 슬라이드에서는 파란색이나 남색 면적을 크게 사용할 수 있다.
- 그라데이션은 파랑에서 시안으로 이어지는 단순한 방향을 사용한다. 강조 수치나 화면 가장자리 중 한 곳에만 적용한다.
- 참고 1의 보라빛 파란색은 별도 테마 후보로 본다. 한 발표 안에서 기존 Cleany 파란색과 이유 없이 교차 사용하지 않는다.
- ‘실물’, ‘시뮬레이션’, ‘예시 데이터’, ‘목표’처럼 의미가 중요한 구분은 색만으로 전달하지 않고 글자로 병기한다.

### 글자

기준 캔버스는 16:9, **13.333 × 7.5인치**다.

| 역할 | 권장 크기 | 스타일 |
|---|---|---|
| 표지·핵심 선언 | 38–48pt | Bold, 짧은 문장, 의도적인 줄바꿈 |
| 일반 슬라이드 제목 | 28–34pt | Bold, 가능하면 한 줄, 필요하면 두 줄 |
| 영역 제목·항목명 | 20–24pt | SemiBold 또는 Bold |
| 주요 본문 | 17–22pt | Regular 또는 Medium |
| 캡션·범례·출처 | 11–14pt | 명확한 대비, 설명을 짧게 유지 |
| 보조 섹션명·쪽 번호 | 9–11pt | 내용을 이해하는 데 필수적이지 않은 정보만 |

- Pretendard 계열을 기본 후보로 사용한다. 실제 편집 환경에서 사용할 수 없으면 설치가 확인된 Noto Sans CJK KR 등 한글 서체로 통일한다.
- 서체 이름을 지정한 것과 실제 글리프가 정상 표시되는 것은 다르다. 한글, 숫자, 기호와 굵기를 렌더링해 확인한다.
- 제목·본문·캡션의 위계는 색상뿐 아니라 크기 차이로도 드러나야 한다.
- 주요 문장 일부만 강조한다. 문장 전체를 굵게 만드는 방식이 연속되지 않게 한다.
- 실제 성과의 범위를 결정하는 조건과 한계는 작은 출처 글씨로 숨기지 않는다.
- 원본처럼 **20 × 11.25인치** 캔버스를 사용할 경우 위 포인트 크기를 **1.5배**로 환산한다. 화면에서 같은 크기로 보이려면 캔버스 확대에 맞춰 글자도 확대해야 한다.
- 넘치는 글자를 자동 축소하는 설정에 의존하지 않는다. 긴 항목은 줄바꿈·문장 정리·영역 재배치로 해결한다.

### 여백과 정렬

- 좌우 안전 여백은 슬라이드 너비의 약 4.5–6%를 시작점으로 잡는다.
- 제목, 본문, 그림의 시작선을 공유한다. 다른 종류의 요소라도 기준선을 맞추면 통일감이 생긴다.
- 제목과 본문 사이에는 분명한 간격을 둔다. 두 줄 제목 아래에 부제나 그림이 붙지 않도록 한다.
- 이미지 두 개를 비교할 때에는 같은 기준으로 크롭하고 상단 또는 하단을 맞춘다.
- 캡션은 해당 이미지에 가깝게 배치한다. 서로 다른 그림의 설명이 어느 쪽인지 모호해지지 않게 한다.
- 여백은 정보 그룹의 경계를 나타내는 데 사용한다. 빈 곳을 장식으로 모두 채우지 않는다.
- 배경 사진이나 색면을 끝까지 채우는 구성에서도 글자는 안전 여백 안에 둔다.

## 4. 카드 사용 기준

### 카드가 적합한 경우

- 팀원, 서비스, 독립된 기능 등 같은 수준의 항목을 비교할 때.
- 항목마다 사진·아이콘·수치가 있어 하나의 묶음으로 읽어야 할 때.
- 여러 후보 중 선택된 항목 하나를 강조할 때.

### 다른 구성이 더 적합한 경우

| 전달하려는 내용 | 우선 사용할 표현 |
|---|---|
| 순서 | 번호, 연결선, 단계별 장면 |
| 원인과 결과 | 짧은 문장과 화살표, 전후 비교 |
| 계층과 책임 | 시스템 구조도, 열 정렬, 연결 관계 |
| 수치 비교 | 같은 축을 쓰는 차트 또는 정렬된 표 |
| 시간에 따른 계획 | 타임라인, 단계별 완료 조건 |
| 실험 증거 | 큰 이미지·영상·그래프와 짧은 해석 |
| 핵심 주장 | 큰 문장, 제한된 색면, 여백 |

### 카드를 사용할 때

- 내용을 먼저 그룹화한 뒤 카드가 필요한지 판단한다.
- 같은 그룹의 카드는 모서리, 안쪽 여백, 제목 위치를 맞춘다.
- 흰색 카드와 옅은 경계 또는 약한 그림자 중 필요한 방식만 선택한다.
- 테두리·채움·그림자를 동시에 강하게 사용하지 않는다.
- 그림자는 실제 카드에만 제한적으로 사용한다. 연결선, 번호, 표의 구분선에는 적용하지 않는다.
- 라운드는 기준 캔버스에서 6–12pt 정도를 시작점으로 잡는다. 모든 도형을 알약 모양으로 만들지 않는다.
- 강조할 카드 하나만 파란색으로 채우고 나머지는 중립색으로 두는 방식을 우선 고려한다.
- 동일 크기 카드가 3개씩 놓인 구성을 여러 장 연속 사용하지 않는다. 카드 개수보다 반복되는 화면 구조가 문제다.

## 5. 레이아웃 선택표

이 표는 고정 템플릿 목록이 아니라 내용에 맞는 구성을 고르는 기준이다.

| 유형 | 추천 구성 | Cleany 적용 예 |
|---|---|---|
| 표지·선언 | 큰 제목 + 로봇 이미지 또는 넓은 파란색 면 | ‘이용자가 떠난 뒤 책상을 정리하는 로봇’ |
| 문제 정의 | 큰 현장 이미지 + 대비되는 업무 설명 + 핵심 근거 | 무인화된 운영과 사람이 맡는 정리 업무 |
| 제품 소개 | 로봇 이미지 중심 + 번호와 짧은 주석 | 이동·인식·수거·보고 |
| 비교 | 정렬된 표 또는 동일 조건의 좌우 이미지 | 기존 해결책 비교, 알고리즘 비교 |
| 시나리오 | 열린 순서도 또는 단계별 이미지 | 좌석 요청부터 보고까지 |
| 시스템 구조 | 연결선이 있는 계층·분기 도식 | Dashboard, Mission Manager, 실행 기능 |
| 기술 결과 | 큰 증거 이미지·차트 + 짧은 해석 | 검출 결과, SLAM 비교, 파지 후보 |
| 동작 시연 | 큰 연속 장면 또는 영상 + 최소한의 단계명 | PICK → LIFT → BIN |
| 판단과 피드백 | 순환 화살표와 역할 구분 | 관찰 → 판단 → 실행 → 결과 확인 |
| 현재 수준과 한계 | 같은 기준으로 읽는 두 열 또는 상태표 | 기능별 검증 범위와 남은 작업 |
| 로드맵 | 세로·가로 타임라인 + 단계·완료 조건 | 실물 통합, 반복 시험, 후속 개발 |
| 팀·독립 기능 소개 | 사진 또는 아이콘을 포함한 선택적 카드 | 역할 분담, 독립 서비스 소개 |
| 마무리 | 핵심 문장 하나 + 절제된 브랜드 요소 | 프로젝트 목표와 다음 이정표 |

일반 설명 슬라이드가 연속될 때에는 큰 이미지, 결과 차트, 핵심 문장 등으로 중심 요소를 바꾼다. 다만 동일 실험을 연속 비교하는 장면은 같은 레이아웃을 유지하는 편이 좋다. 변화 자체보다 이해와 비교가 우선이다.

## 6. 이미지·도식·장식

### 이미지와 영상

- 가능한 한 프로젝트의 실제 사진, 실제 UI, 실제 실험 결과를 사용한다.
- 제품 형태를 설명할 때는 로봇 전체를, 파지 동작을 설명할 때는 그리퍼와 물체를 크게 보여준다.
- 스크린샷을 통째로 축소하기 전에 전달할 부분을 정한다. 필요하면 전체 화면과 확대 영역을 나란히 배치한다.
- 전체 슬라이드를 축소했을 때에도 주인공인 물체나 변화가 보여야 한다.
- 비교 이미지의 시점·배율을 맞춘다. 시점이 다르면 표시하거나 설명한다.
- 크롭으로 제목이 반쯤 남거나 축·단위가 사라지지 않도록 한다. 그림 내부 제목이 불필요하면 깔끔하게 제외하고 별도의 편집 가능한 제목을 붙인다.
- 시뮬레이션, 실물, CAD, 예시 데이터의 구분은 이미지 옆에서 읽을 수 있어야 한다.
- 생성 이미지나 일반 사진을 실제 구현·실험 증거처럼 사용하지 않는다.

### 도식과 아이콘

- 연결선은 순서·관계·정보 흐름을 나타낼 때만 사용한다. 방향이 의미와 일치해야 한다.
- 아이콘은 기능을 빠르게 이해하도록 돕는 경우에 사용한다. 장식 목적의 아이콘을 모든 항목에 붙이지 않는다.
- 한 발표에서는 선형 또는 면형 아이콘의 스타일을 통일한다.
- 의미 없는 입체 도형 대신 편집 가능한 단순 도형과 연결선을 우선 사용한다.
- 시스템 구조를 단순화하더라도 주요 책임 경계나 입력·출력 관계를 바꾸지 않는다.

### 배경 장식

- 참고 2·3의 흐릿한 파란색 면과 가는 물결 선은 선택적 장식이다. 필수 구성 요소가 아니다.
- 장식을 쓰면 모서리나 가장자리로 제한하고, 표·차트·본문 뒤를 지나지 않게 한다.
- 한 장에 그라데이션, 물결 선, 강한 그림자, 큰 색면을 모두 넣지 않는다.
- 표지·장 구분·마무리에서 장식을 더 활용하고, 검증 결과 장에서는 증거가 중심이 되게 한다.

## 7. 표·차트·핵심 수치

- 표는 행과 열의 비교를 우선한다. 셀마다 카드처럼 그림자나 둥근 테두리를 넣지 않는다.
- 제목 행, 핵심 행, 나머지 행의 세 단계 정도로 강조를 제한한다.
- 같은 지표는 단위와 소수점 자릿수를 일관되게 표시한다.
- 차트에는 필요한 축, 단위, 범례, 시험 조건을 남긴다. 배경 그리드는 데이터보다 약하게 표현한다.
- 한 결과를 강조할 때 나머지 비교 대상도 읽을 수 있게 남긴다.
- 수치가 없는 곳에 장식용 상승 그래프, 성공률, 진행률을 만들지 않는다.
- 핵심 숫자를 크게 쓸 때에는 ‘무엇을 측정한 값인지’를 바로 옆에 둔다.
- 예를 들어 ATE는 궤적 평가 수치이고 그리퍼 벌림 폭은 선택된 동작 값이다. 디자인 과정에서 이를 도착 정확도나 파지 성공률로 바꾸어 표현하지 않는다.
- 단일 시연, 반복 실험, 시뮬레이션 검증, 실물 통합 검증의 차이를 시각적 강조로 흐리지 않는다.

## 8. 현재 Cleany 자료에 적용하는 방향

다음은 현재 발표자료를 수정할 때의 디자인 방향이다. 문구와 제품 범위를 확정하거나 자동으로 바꾸라는 지시가 아니다.

| 대상 | 우선 개선 |
|---|---|
| 2장 문제 정의 | 카드 제거에 그치지 않고 이미지·두 업무 영역·근거 수치의 위계를 만든다. 문제 현장 사진이 확보되면 배치 후보로 검토한다. |
| 3장 기존 해결책 | 비교표의 정렬과 핵심 행을 중심으로 구성한다. 상단 단계 설명을 표보다 과하게 강조하지 않는다. |
| 4장 시장·적용 경로 | 첫 적용 시장을 크게 두고 확장 관계를 분기나 연결선으로 보여준다. |
| 5장 기술 배경 | ‘본다·판단한다·행동한다’를 큰 문장 또는 간단한 흐름으로 보여준다. |
| 7장 시나리오 | 여섯 개의 동일 카드 대신 번호와 흐름, 또는 의미 있는 단계 이미지를 사용한다. |
| 8장 시스템 | 단순한 상자 적층보다 구성 요소의 연결과 책임 관계를 읽을 수 있게 한다. |
| 9장 Dashboard | 좌석 지정과 작업 결과의 핵심 영역을 크게 보여준다. 예시 데이터 표기를 유지한다. |
| 10–12장 기술 결과 | 실험 그림과 표를 우선 배치하고, 설명은 결과 해석을 돕는 정도로 줄인다. |
| 13장 수거 | 물체 위치 변화를 식별할 수 있는 연속 장면 또는 영상 중심으로 구성한다. |
| 14–15장 확장 구조 | 현재 구성과 목표 구성의 구분, 판단과 실행의 관계를 도식으로 보여준다. |
| 16장 전체 흐름 | 같은 미션의 연결 증거인지 단계별 결과 모음인지 구분하면서 장면 중심으로 구성한다. |
| 17장 구현 수준 | 항목마다 구현 범위·검증 환경·한계가 같은 기준으로 읽히게 한다. |
| 18장 로드맵 | 단계별 흐름을 읽을 수 있는 타임라인으로 바꾸고, 실제로 제공된 완료 조건을 표시한다. |

## 9. 실시간 편집 방식

- 현재 열린 Impress 문서가 작업 원본이다. 이전에 생성한 PPTX나 빌드 스크립트로 사용자 변경을 덮어쓰지 않는다.
- 수정 전에 현재 문서, 선택 슬라이드, 개체와 미저장 변경 상태를 읽는다.
- ‘2장만’과 같이 범위가 지정되면 해당 장만 수정한다.
- 의미 있는 변경 전에는 현재 미저장 상태를 포함한 복구 사본을 만든다.
- 대상 슬라이드를 화면에 띄우고, 한 번에 검토 가능한 정도의 변경을 반영한다.
- 사용자는 같은 화면에서 피드백하고, 다음 수정은 그 화면의 최신 상태를 기준으로 진행한다.
- 대표 장에서 스타일을 맞춘 후에 다른 장으로 확장한다. 한 장의 방향 확인을 전체 자료 수정 승인으로 해석하지 않는다.
- 매번 새 파일을 전달하기보다 같은 작업본과 같은 미리보기를 갱신한다. 백업은 작업 화면과 별도로 관리한다.
- 저장 방식은 현재 세션에서 합의한 방침을 따른다. 편집 반영과 파일 저장을 구분해 알린다.
- 최종 확정 단계에서 PPTX·PDF를 정리하고 전체 슬라이드를 검사한다.

## 10. 완료 전 확인

### 시각적 구성

- [ ] 이 장에서 가장 먼저 보이는 요소가 핵심 메시지와 일치한다.
- [ ] 카드·문장 목록 외에 내용에 맞는 시각적 형식을 검토했다.
- [ ] 카드가 있으면 독립 항목을 묶는 명확한 이유가 있다.
- [ ] 앞뒤 장과 같은 구성만 반복되지 않는다. 비교를 위해 반복했다면 의도적이다.
- [ ] 제목, 이미지, 본문의 정렬과 그룹 사이 여백이 일관된다.
- [ ] 작은 화면에서도 중심 이미지와 핵심 수치를 구분할 수 있다.

### 가독성과 정확성

- [ ] 한글, 숫자, 기호와 굵기가 실제 편집 환경에서 정상 표시된다.
- [ ] 텍스트가 잘리거나 이미지·구분선과 겹치지 않는다.
- [ ] 자동 축소로 본문이 지나치게 작아지지 않았다.
- [ ] 그림의 제목·축·단위·중요 객체가 크롭으로 손상되지 않았다.
- [ ] 실물·시뮬레이션·예시·목표와 검증 조건을 읽을 수 있다.
- [ ] 디자인을 위해 수치나 구현 상태를 새로 만들거나 의미를 바꾸지 않았다.

### 편집과 전달

- [ ] 요청된 슬라이드 범위만 변경했다.
- [ ] 제목·본문·주요 도식은 편집 가능한 요소로 유지했다.
- [ ] 사용자 미저장 변경과 복구 사본을 보존했다.
- [ ] 실제 화면 또는 렌더링을 확인했다. 파일 생성 성공만으로 검증을 대신하지 않았다.
- [ ] 최종 PPTX와 PDF의 장수·폰트·레이아웃을 확인했다.
- [ ] 정적 렌더링 확인과 PowerPoint 재생·발표장 투사 확인을 구분했다.

## 11. 후속 편집에 사용할 짧은 지시문

> 이 design.md를 기준으로 요청된 슬라이드만 수정한다. 밝은 바탕과 파란색 강조, 강한 제목과 여백을 유지하되, 내용에 따라 큰 이미지·비교표·흐름도·타임라인을 선택한다. 카드는 독립 항목을 묶는 경우에만 사용하고, 제거한 자리를 작은 텍스트 나열로 대체하지 않는다. 실제 자료와 수치, 검증 범위를 보존한다. 열린 Impress의 최신 상태를 읽고 수정하며, 적용 후 화면을 확인한다.
